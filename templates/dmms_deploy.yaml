apiVersion: v1
kind: Deployment
metadata:
  name: pandas-dmms
  labels:
    name: pandas-dmms
spec:
  replicas: 1
  template:
    metadata:
      labels: 
       app: pandas-dmms
    spec:
      containers:
      - name: pandas-dmms
        image: 192.168.1.101:5000/dmms:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
          name: httpport
#        env:
#          -name: GOROOT
#          value: /usr/local/go
        livenessProbe:
          initialDelaySeconds: 15
          periodSeconds: 20
#          httpGet:
#            path: /
#            port: 8080
          tcpSocket:
            port: 8080
        readinessProbe:
          initialDelaySeconds: 5
          periodSeconds: 10
#          httpGet:
#            path: /
#            port: 8080
          tcpSocket:
            port: 8080