package message

import "testing"

func Test_SendMessage(t *testing.T) {
	/*
		factory := &builtinFactory{}
		msg := factory.CreateMessage(TopicNameDevice)
		msg.SetTopic("abc")
		if err := SendMessage("localhost:9092", msg); err != nil {
			t.Fatal(err)
		}
	*/
}
